<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>User Movie Dashboard</title>

  <!-- Bootstrap & Font Awesome -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet"/>

  <style>
    body { overflow-x: hidden; background-color: #f8f9fa; }
    .sidebar { min-height: 100vh; }
    .sidebar .nav-link { color: #fff; cursor: pointer; }
    .sidebar .nav-link.active { background-color: #343a40; border-radius: 0.375rem; }
    .navbar-brand { color: #fff; }

    /* Dynamic Content Scroll */
    .dynamic-content {
      overflow-y: auto;
      max-height: 85vh;
      padding: 20px;
    }

    .content-section {
      display: none; /* Hide all sections initially */
    }

    .content-section.active {
      display: block; /* Show only the selected section */
    }
     /* ✅ Ensure Consistent Card Sizes */
   /* ✅ Ensure Consistent Card Sizes */
  .movie-card {
    width: 100%;
    max-width: 300px;
    height: 450px;
    margin: auto;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: space-between;
    border-radius: 8px; /* Rounded corners */
  }

  /* ✅ Maintain Image's Full Size */
  .movie-card img {
    width: 100%;
    height: 250px;
    object-fit: scale-down; /* ✅ Ensures full image is displayed without cropping */
    background-color: black; /* ✅ Adds background for smaller images */
  }

  .movie-card .card-body {
    text-align: center;
    flex-grow: 1; /* ✅ Keeps spacing consistent */
  }
/* Add hover effect to movie cards */
.movie-card:hover {
  transform: scale(1.02);
  box-shadow: 0 8px 16px rgba(0,0,0,0.3);
  transition: transform 0.3s, box-shadow 0.3s;
}

  </style>
</head>
<body>

  <!-- ✅ Mobile Navbar -->
  <nav class="navbar navbar-dark bg-dark d-md-none">
    <div class="container-fluid">
      <button class="navbar-toggler" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasSidebar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <span class="navbar-brand">User Dashboard</span>
    </div>
  </nav>

  <div class="container-fluid">
    <div class="row">
      <!-- ✅ Sidebar Navigation (Static) -->
      <nav id="offcanvasSidebar" class="col-md-2 col-lg-2 d-md-block bg-black sidebar offcanvas-md offcanvas-start text-white p-3">
        <h4 class="text-center">👤 Welcome, <%=viewprof[0].username%></h4>
        <hr />
        <ul class="nav flex-column">
          <li class="nav-item"><a href="/" class="nav-link active" data-section="homeSection"><i class="fas fa-home me-2"></i>Home</a></li>
          <li class="nav-item"><a class="nav-link" data-section="searchSection"><i class="fas fa-search me-2"></i>Search</a></li>
          <li class="nav-item"><a href="/userviewmove" class="nav-link" data-section="moviesSection"><i class="fas fa-video me-2"></i>View Movies</a></li>
          <li class="nav-item"><a class="nav-link" data-section="detailsSection"><i class="fas fa-info-circle me-2"></i>View Profile</a></li>
          <li class="nav-item"><a class="nav-link" data-section="subscribeSection"><i class="fas fa-bell me-2"></i>Subscribe</a></li>
          <li class="nav-item"><a href="/ratinguser" class="nav-link" data-section="rateSection"><i class="fas fa-star me-2"></i>Rating</a></li>
          <li class="nav-item"><a href="/watchlist" class="nav-link" data-section="commentSection"><i class="fas fa-comment me-2"></i>Watchlist</a></li>
          <li class="nav-item"><a class="nav-link" href="/"><i class="fas fa-sign-out-alt me-2"></i>Logout</a></li>
        </ul>
      </nav>

      <!-- ✅ Dynamic Content (Scroll Enabled) -->
      <main class="col-md-10 ms-sm-auto col-lg-10 px-md-4 pt-4">
        <div class="dynamic-content">
          <div id="homeSection" class="content-section active">
            <h2>Welcome to the User Dashboard</h2>
            <p>Select an option from the left sidebar to begin.</p>
          </div>

          <div id="searchSection" class="content-section">
            <h2>🔍 Search Movies</h2>
            <p>Find your favorite movies here.</p>
          </div>

          <!-- ✅ View Movies Section with Dynamic Card Grid -->
         <div id="moviesSection" class="content-section">
           
            <div class="container">
             <div class="row" id="moviesContainer">
           <!-- Movies will load here dynamically -->
             </div>
            </div>
         </div>

          <div id="detailsSection" class="content-section">
            <div class="container mt-5">
              <div class="row justify-content-center">
                <div class="col-md-8 col-lg-6">
                  <div class="card shadow-lg border-0 rounded-4">
                    <div class="card-header bg-primary text-white text-center rounded-top-4">
                      <h3 class="mb-0">👤 User Profile</h3>
                    </div>
                    <div class="card-body bg-light">
                      <form>
                        <div class="mb-3"><label class="form-label fw-semibold">Username</label>
                          <input type="text" class="form-control" value="<%= viewprof[0].username %>" readonly>
                        </div>
                        <div class="mb-3"><label class="form-label fw-semibold">Email</label>
                          <input type="email" class="form-control" value="<%= viewprof[0].email %>" readonly>
                        </div>
                        <div class="mb-3"><label class="form-label fw-semibold">Contact</label>
                          <input type="text" class="form-control" value="<%= viewprof[0].contact %>" readonly>
                        </div>
                        <div class="mb-3"><label class="form-label fw-semibold">City</label>
                          <input type="text" class="form-control" value="<%= viewprof[0].city %>" readonly>
                        </div>
                      </form>
                    </div>
                    <div class="card-footer text-center bg-white border-0">
                      <a href="/updateuser?userid=<%= viewprof[0].uid %>" class="btn btn-outline-primary px-4">✏️ Edit Profile</a>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </main>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <!-- ✅ JavaScript for Sidebar Navigation -->
  <script>
  document.querySelectorAll('.nav-link[data-section]').forEach(link => {
    link.addEventListener('click', function () {
      document.querySelectorAll('.nav-link').forEach(l => l.classList.remove('active'));
      this.classList.add('active');

      document.querySelectorAll('.content-section').forEach(sec => sec.classList.remove('active'));
      const sectionId = this.getAttribute('data-section');
      document.getElementById(sectionId)?.classList.add('active');

    });
  });


  </script>
</body>
</html>
